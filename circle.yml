machine:
  node:
    version: 4.4.4

dependencies:
  cache_directories:
    - 'node_modules'

test:
  post:
    - npm run build

deployment:
  production:
    branch: master
    commands:
      - aws s3 sync dist s3://www.amowu.com/ --delete